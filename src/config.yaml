requiredModules:
  content: true
  mongo: true
  storage: true
  worlds: true
  llmProviders: true
  admin: true
  api: true
  chats: true

projects:
  test_project:
    .storageProviders:
      .mongo:
        db: test_project
      .mongoMemory:
        db: test_project
    telegramServers:
      main:
        id: main
        apiTokenKeyInEnv: TELEGRAM_API_TOKEN
        handlerId: play.gameChatTelegram
  cloudshire:
    .storageProviders:
        .mongo:
          db: cloudshire
        .mongoMemory:
          db: cloudshire
httpServer:
  urlMask: '/*/api/*'
