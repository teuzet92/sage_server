requiredModules:
  content: true
  mongo: true
  storage: true
  shards: true
  llmProviders: true
  admin: true
  api: true

projects:
  test_project:
    .mongo:
      db: test_project
    .mongoMemory:
      db: test_project
    telegramServers:
      main:
        id: main
        apiTokenKeyInEnv: TELEGRAM_API_TOKEN
        handlerId: shards.gameChatTelegram
httpServer:
  urlMask: '/*/api/*'
